# 3. Подсистема 1: Modbus Poller

## 3.1 Функциональные требования

- Опрос регистров функцией **0x03 (Read Holding Registers)** — и значения, и статусы
  (`poller/modbus_client.py: read_holding_registers_raw`); функция 0x04 реализована в клиенте,
  но в цикле опроса не используется
- **Адреса регистров значений:** 30000 + N (`value_register_base`)
- **Адреса регистров статусов:** 40000 + N (`status_register_base`)
- Чётный адрес - температура, нечётный - влажность
- **Пересчёт значений:** формат Q8.8 — знаковое 16-битное значение делится на 256
  (`_signed_16(raw) / 256.0`), результат округляется до 4 знаков. Пример: `0x1980` → 25.5 °C,
  `0xF380` → −12.5 °C
- Непрерывный опрос без остановки
- Логирование обмена по Modbus
- Настройка одной или нескольких линий опроса
- Поддержка линий типа `serial`, `udp_rtu` и `udp_c2000pp`
- Привязка каждого датчика к конкретной линии опроса через уникальное имя порта
- Сохранение в текущих данных и журнале не только Modbus-адреса, но и источника опроса

## 3.2 Параметры подключения (настраиваемые)

Параметры делятся на общие параметры цикла и параметры отдельных линий опроса.

| Параметр | По умолчанию | Диапазон |
|----------|--------------|----------|
| Период опроса | 1000 мс | 100-60000 мс |
| Таймаут | 500 мс | 100-10000 мс |
| Количество повторов | 3 | 0-10 |
| Базовый адрес значений | 30000 | 0-65535 |
| Базовый адрес статусов | 40000 | 0-65535 |
| Максимум записей Modbus-лога | 1000 | 1-50000 |

Каждая линия опроса имеет обязательный `id`, человекочитаемое уникальное `name`, тип транспорта и собственные параметры подключения. `name` используется в интерфейсе и в нумерации датчиков, поэтому должен быть стабильным: например `COM8`, `PP-SKLAD-UDP`, `UDP-192.168.66.100`.

| Тип линии | Назначение | Обязательные поля |
|-----------|------------|-------------------|
| `serial` | Опрос С2000-ПП через COM-порт как Modbus RTU | `com_port`, `baudrate`, `bytesize`, `parity`, `stopbits` |
| `udp_rtu` | Нативный RTU-over-UDP: UDP-датаграмма содержит чистый Modbus RTU кадр с CRC16 | `remote_host`, `remote_port`, опционально `local_host`, `local_port` |
| `udp_c2000pp` | RTU-over-UDP через 5-байтовую обёртку преобразователя | `remote_host`, `remote_port`, `local_host`, `local_port`, `seq_start`, `prefix`, `channel` |

### 3.2.1 UDP-обёртка С2000-ПП

Для транспорта `udp_c2000pp` каждый готовый Modbus RTU кадр, включая CRC16, перед отправкой заворачивается в один UDP-датаграмм с 5-байтовым заголовком:

```
 +------+------+--------+------+===============================+
 | 0x10 | LEN  |  SEQ   | 0x10 |   Modbus RTU frame (LEN байт) |
 +------+------+--------+------+===============================+
 байт0  байт1  байт2-3  байт4   байт5 ... байт(5+LEN-1)
```

| Поле | Размер | Значение | Назначение |
|------|--------|----------|------------|
| `prefix` | 1 байт | всегда `0x10` | Маркер начала пакета |
| `LEN` | 1 байт | длина вложенного RTU-кадра | Количество байт после заголовка |
| `SEQ` | 2 байта, big-endian | счётчик пакетов, например `0x21D1`, `0x21D2`, ... | Информационный номер пакета стороны-отправителя |
| `channel` | 1 байт | всегда `0x10` | Канал/адрес преобразователя |
| `payload` | `LEN` байт | Modbus RTU кадр + CRC16 | Полезная нагрузка |

Правила обмена:

- отправка: верхний слой формирует обычный Modbus RTU кадр с CRC16, транспортный слой добавляет заголовок `prefix/LEN/SEQ/channel` и отправляет обёрнутую UDP-датаграмму на `remote_host:remote_port`;
- приём: транспортный слой принимает следующую UDP-датаграмму в той же обёртке `prefix/LEN/SEQ/channel`, проверяет заголовок, логирует приборный `SEQ`, проверяет `LEN`, снимает 5 байт заголовка и только после этого отдаёт чистый Modbus RTU кадр верхнему разбору;
- чистый RTU-ответ без 5-байтовой обёртки для транспорта `udp_c2000pp` считается ошибкой протокола и логируется как `invalid_udp_wrapper`;
- локальный порт: для штатной схемы ПК `192.168.66.97:40005` и прибор `192.168.66.100:40005`, поэтому UDP-сокет должен уметь bind на заданные `local_host` и `local_port`;
- TX SEQ: хранится отдельно на каждую UDP-линию, увеличивается на 1 на каждый запрос, при переполнении переходит по модулю `0x10000`;
- RX SEQ: не является эхом запроса; С2000-ПП/преобразователь ведёт собственный независимый счётчик ответных датаграмм, поэтому RX SEQ только логируется и не сравнивается с TX SEQ;
- сопоставление ответа: канал используется как синхронный COM-порт поверх UDP, ровно один запрос -> один следующий ответ; перед отправкой нового запроса socket очищает протухшие UDP-датаграммы, а корреляция выполняется по вложенному Modbus RTU кадру: slave id, function/function|0x80, длина и CRC16;
- проверка заголовка: `prefix` и `channel` в принятой обёртке должны соответствовать настройкам линии, по умолчанию `0x10` и `0x10`;
- LEN: в ответе проверяется отдельно и должен совпадать с длиной вложенного RTU-кадра;
- распаковка: после проверки заголовка верхнему Modbus-разбору передаётся только чистый RTU-кадр;
- CRC16: обёртка не меняет и не заменяет Modbus CRC16; после распаковки CRC проверяется тем же кодом, что и для COM-порта;
- таймаут: если до истечения `timeout_ms` не пришла следующая валидная обёрнутая датаграмма с корректным Modbus RTU кадром, запрос считается неуспешным и проходит обычную логику повторов.

### 3.2.2 Многоканальный опрос

Опросчик должен обслуживать несколько линий опроса в одном процессе через менеджер портов. Опрос каждой линии `serial`, `udp_rtu` или `udp_c2000pp` должен выполняться независимо: отдельный worker, отдельное транспортное состояние, отдельная статистика и отдельный жизненный цикл подключения. Последовательный общий цикл по всем портам не допускается как финальная реализация, потому что таймаут одного С2000-ПП не должен задерживать остальные COM/UDP-линии.

#### 3.2.2.1 Менеджер портов опроса

`PollPortManager` является центральным компонентом Modbus Poller и управляет всеми линиями опроса.

Обязанности менеджера:

- загрузить и провалидировать список `poll_ports[]`;
- создать worker на каждую включённую линию;
- передать каждому worker только датчики его `poll_port_id`;
- запускать, останавливать и перезапускать отдельную линию без остановки остальных;
- хранить реестр состояний портов: `configured`, `starting`, `running`, `degraded`, `error`, `stopped`, `disabled`;
- агрегировать `current.json` из последних снимков всех workers;
- агрегировать `modbus_log.json`, сохраняя в каждой записи `poll_port_id`, `poll_port_name` и transport metadata;
- принимать изменения конфигурации из API и UI, вычислять diff и перезапускать только затронутые линии;
- не допускать удаления линии, к которой привязаны датчики;
- отдавать одинаковую модель данных в Modbus Poller UI и в Visualizer `/settings/poller`.

Worker линии:

- владеет одним COM handle или одним UDP socket;
- для `udp_c2000pp` владеет своим TX-счётчиком SEQ, логирует независимый RX-счётчик прибора и очищает протухшие UDP-датаграммы перед новым запросом;
- выполняет цикл опроса только своих датчиков;
- помечает offline только свои датчики;
- пишет собственные ошибки в состояние порта, не меняя состояние остальных портов;
- поддерживает остановку и reconnect между циклами без потери состояния других линий.

#### 3.2.2.2 Пер-линейные параметры опроса и «медленный цикл»

Каждая линия опроса имеет собственные параметры опроса (пер-линейные значения переопределяют общие):

| Поле линии | По умолчанию | Значение |
|------------|--------------|----------|
| `poll_period_ms` | `0` | Период опроса линии, мс; `0` — использовать общий `poll_period_ms` |
| `timeout_ms` | `500` | Таймаут ожидания ответа по линии, мс |
| `retry_count` | `-1` | Число повторов запроса к датчику до перевода его в «медленный цикл»; `-1` — использовать общий `retry_count` |
| `slow_poll_period_ms` | `30000` | Период «медленного цикла»: как часто опрашивается датчик, переведённый в медленный цикл |

Алгоритм «медленного цикла» (датчик никогда не выпадает из опроса):

1. Датчик опрашивается в обычном цикле линии; при ошибке выполняется до `retry_count` повторов.
2. ЕСЛИ все повторы исчерпаны, датчик переводится в «медленный цикл»: опрашивается одной попыткой раз в `slow_poll_period_ms`, чтобы не тормозить опрос отвечающих датчиков таймаутами.
3. Между медленными попытками датчик ОСТАЁТСЯ в выдаче `current.json` (последний снимок/offline, признак `slow_poll: true`) — из опроса и визуализации он не пропадает никогда.
4. ПРИ первом успешном ответе датчик автоматически возвращается в обычный (быстрый) цикл.

Список датчиков в медленном цикле отдаётся в статусе линии (`slow_poll_sensors`), там же — действующие `poll_period_ms`/`retry_count`/`slow_poll_period_ms`.

#### 3.2.2.3 Гарантия непрерывности опроса

Опрос НИКОГДА не останавливается самостоятельно — ни при каких ошибках (таймауты, сбой открытия/чтения порта, ошибки записи журнала, переполнение лога, внутренние исключения). Требования к реализации:

- цикл worker полностью защищён: любая ошибка логируется в состояние линии (`degraded`/`error`), и цикл продолжается; единственный способ остановки — явная команда оператора (stop);
- сторож (watchdog) каждые 5 секунд проверяет живость workers и автоматически пересоздаёт упавший worker линии;
- журнал Modbus ограничен настройкой `log_max_entries` ЦЕЛИКОМ (записи, TX, RX и журнал обменов) и перезатирается по кольцу — рост файла `modbus_log.json` не может замедлить или остановить опрос.

Требования:

- датчики группируются по `poll_port_id`;
- на каждую активную линию создаётся отдельный Modbus-клиент и отдельное транспортное состояние;
- для COM-линий открывается отдельный serial handle;
- для UDP-линий создаётся отдельный UDP socket с собственным `local_host/local_port`;
- статистика ведётся глобально и по каждой линии: `total_polls`, `successful_polls`, `failed_polls`, `timeouts`, `crc_errors`, `last_error`, `last_poll_at`, `last_success_at`;
- перезагрузка конфигурации не должна ронять весь сервис: изменённая линия переподключается между циклами, остальные продолжают работать;
- если линия недоступна, offline получают только датчики этой линии.

### 3.2.3 Нумерация и уникальность датчиков

Глобальный `sensor.id` остаётся внутренним неизменяемым идентификатором для архива, ссылок и API. В пользовательском интерфейсе дополнительно отображается номер датчика внутри линии опроса:

```
<poll_port.name>-<sensor.local_number>
```

Примеры: `COM8-001`, `COM8-002`, `UDP-192.168.66.100-001`, `PP-SKLAD-UDP-014`.

Правила:

- `poll_port.name` должен быть уникальным среди всех линий;
- `local_number` уникален только внутри своей линии;
- пара `(poll_port_id, modbus_slave_id, modbus_addr_temp, modbus_addr_hum)` должна быть уникальной;
- одинаковые Modbus-адреса допустимы на разных линиях;
- при миграции старой конфигурации всем существующим датчикам назначается линия `default`, а `local_number` вычисляется по текущему порядку датчиков.

## 3.3 Структура датчика С2000-ВТ

Один датчик С2000-ВТ занимает 2 адреса Modbus:

**Регистры значений (30000 + N):**
- **Адрес N (чётный):** Температура (°C × 10, знаковое 16-бит)
- **Адрес N+1 (нечётный):** Влажность (% × 10, беззнаковое 16-бит)

**Регистры статусов (40000 + N):**
- **Адрес N (чётный):** Статус канала температуры
- **Адрес N+1 (нечётный):** Статус канала влажности

## 3.4 Формат выходных данных (current.json)

> **Примечание:** Название датчика (`name`) задаётся пользователем в разделе **Настройки системы → Настройки датчиков**. Опросчик получает конфигурацию датчиков из файла `system_config.json`.

```json
{
  "timestamp": "2026-01-14T15:30:45.123456",
  "poll_period_ms": 1000,
  "poll_ports": [
    {
      "id": "default",
      "name": "COM8",
      "transport": "serial",
      "status": "running",
      "last_success_at": "2026-01-14T15:30:45.120000",
      "last_error": null
    },
    {
      "id": "pp_udp_1",
      "name": "UDP-192.168.66.100",
      "transport": "udp_c2000pp",
      "remote_host": "192.168.66.100",
      "remote_port": 40005,
      "local_host": "192.168.66.97",
      "local_port": 40005,
      "seq_current": "21D2",
      "status": "running",
      "last_success_at": "2026-01-14T15:30:45.120000",
      "last_error": null
    }
  ],
  "sensors": [
    {
      "id": 1,
      "poll_port_id": "pp_udp_1",
      "poll_port_name": "UDP-192.168.66.100",
      "display_number": "UDP-192.168.66.100-001",
      "local_number": 1,
      "name": "ХРАН. № 1",
      "modbus_slave_id": 16,
      "modbus_addr_temp": 1,
      "modbus_addr_hum": 2,
      "temperature": {
        "value": 23.5,
        "raw": 235,
        "status": "ok",
        "modbus_status": 0,
        "timestamp": "2026-01-14T15:30:45.100000"
      },
      "humidity": {
        "value": 45.2,
        "raw": 452,
        "status": "ok",
        "modbus_status": 0,
        "timestamp": "2026-01-14T15:30:45.120000"
      },
      "combined_status": "normal"
    }
  ],
  "statistics": {
    "total_polls": 12345,
    "successful_polls": 12340,
    "failed_polls": 5,
    "last_error": null,
    "ports": {
      "default": {"total_polls": 1000, "failed_polls": 0, "last_error": null},
      "pp_udp_1": {"total_polls": 900, "failed_polls": 2, "last_error": null}
    }
  }
}
```

## 3.5 Статусы датчиков (коммуникационные)

| Статус | Код | Описание |
|--------|-----|----------|
| ok | 0 | Данные получены успешно |
| timeout | 1 | Таймаут ответа |
| crc_error | 2 | Ошибка CRC |
| exception | 3 | Modbus exception |
| offline | 4 | Датчик недоступен (N попыток) |

## 3.6 Комбинированные статусы (combined_status)

| Статус | Описание |
|--------|----------|
| normal | Значения в норме |
| warning_high_temp | Превышение верхней границы температуры |
| warning_low_temp | Принижение нижней границы температуры |
| warning_high_hum | Превышение верхней границы влажности |
| warning_low_hum | Принижение нижней границы влажности |
| alarm | Критическое отклонение (более X% от границы) |
| no_connection | Нет связи с датчиком |
| guarded | На охране (мониторинг границ активен) |

## 3.7 Формат лога Modbus (modbus_log.json)

```json
{
  "max_entries": 1000,
  "entries": [
    {
      "timestamp": "2026-01-14T15:30:45.100000",
      "direction": "TX",
      "raw_hex": "10 03 75 30 00 02 XX XX",
      "parsed": {
        "slave_id": 16,
        "function": 3,
        "start_addr": 30000,
        "quantity": 2,
        "description": "Запрос значений датчика 1"
      }
    },
    {
      "timestamp": "2026-01-14T15:30:45.120000",
      "direction": "RX",
      "raw_hex": "10 03 04 00 EB 01 C4 XX XX",
      "parsed": {
        "slave_id": 16,
        "function": 3,
        "byte_count": 4,
        "values": [235, 452],
        "description": "Ответ: T=0.9180°C, H=1.7656%"
      }
    },
    {
      "timestamp": "2026-01-14T15:30:45.150000",
      "direction": "TX",
      "raw_hex": "10 03 9C 40 00 02 XX XX",
      "parsed": {
        "slave_id": 16,
        "function": 3,
        "start_addr": 40000,
        "quantity": 2,
        "description": "Запрос статусов датчика 1"
      }
    },
    {
      "timestamp": "2026-01-14T15:30:45.170000",
      "direction": "RX",
      "raw_hex": "10 03 04 00 00 00 00 XX XX",
      "parsed": {
        "slave_id": 16,
        "function": 3,
        "byte_count": 4,
        "values": [0, 0],
        "description": "Ответ: статусы OK"
      }
    }
  ]
}
```

API `/api/poller/log` дополнительно возвращает очереди `tx_queue`, `rx_queue` и `exchange_queue`. Очередь `exchange_queue` является основным журналом диагностики: каждая запись содержит `source.config_line`, `source.sensor_id`, `source.poll_port_id`, `source.poll_port_name`, `source.register_group`, `tx_hex`, `rx_hex`, `response_time_ms`, `status` и `result`, чтобы в одном фрейме UI было видно соответствие запроса, ответа и строки конфигурации.

Для UDP-линий в журнал добавляются транспортные поля:

```json
{
  "transport": "udp_c2000pp",
  "poll_port_id": "pp_udp_1",
  "poll_port_name": "UDP-192.168.66.100",
  "udp": {
    "local": "192.168.66.97:40005",
    "remote": "192.168.66.100:40005",
    "tx_seq": "21D1",
    "rx_seq": "4644",
    "wrapped_tx_hex": "10 08 21 D1 10 10 03 75 30 00 02 CA 1B",
    "wrapped_rx_hex": "10 09 21 D1 10 10 03 04 00 EB 01 C4 1A 2B",
    "payload_tx_hex": "10 03 75 30 00 02 CA 1B",
    "payload_rx_hex": "10 03 04 00 EB 01 C4 1A 2B"
  }
}
```

`tx_hex` и `rx_hex` в общих полях всегда остаются чистыми Modbus RTU кадрами, чтобы существующие разборщики CRC и UI не зависели от транспорта. Полные UDP-датаграммы пишутся только в `udp.wrapped_tx_hex` и `udp.wrapped_rx_hex`.

## 3.8 API Modbus Poller (REST)

| Метод | Endpoint | Описание |
|-------|----------|----------|
| GET | /api/poller/status | Текущий статус опросчика |
| GET | /api/poller/current | Текущие значения (current.json) |
| GET | /api/poller/log | Лог обмена Modbus |
| POST | /api/poller/config | Изменить конфигурацию |
| POST | /api/poller/start | Запустить опрос |
| POST | /api/poller/stop | Остановить опрос |
| GET | /api/poller/ports | Список доступных COM-портов |
| GET | /api/poller/poll-ports | Список настроенных линий опроса и их статусы |
| POST | /api/poller/poll-ports | Создать или обновить линию опроса |
| DELETE | /api/poller/poll-ports/{id} | Удалить линию опроса, если на неё не привязаны датчики |
| POST | /api/poller/poll-ports/{id}/start | Запустить опрос одной линии |
| POST | /api/poller/poll-ports/{id}/stop | Остановить опрос одной линии |
| POST | /api/poller/poll-ports/{id}/restart | Перезапустить подключение одной линии |
| GET | /api/poller/poll-ports/{id}/log | Лог обменов одной линии |
| GET | /api/poller/scan?poll_port_id=&start_id=&end_id=&timeout_ms= | Поиск Modbus устройств (Slave ID) на выбранной линии с заданным таймаутом |
| POST | /api/poller/reload | Перезагрузить конфигурацию датчиков |
| GET | /api/mockserver/status | Статус тестового Mock Server |
| POST | /api/mockserver/start | Запустить тестовый Mock Server |
| POST | /api/mockserver/stop | Остановить тестовый Mock Server |

## 3.9 Конфигурация Modbus Poller (poller_config.json)

```json
{
  "auto_start": true,
  "use_mock_server": false,
  "mock_server_url": "http://0.0.0.0:8000",
  "poll_ports": [
    {
      "id": "default",
      "name": "COM8",
      "enabled": true,
      "transport": "serial",
      "com_port": "COM8",
      "device_slave_id": 16,
      "baudrate": 9600,
      "bytesize": 8,
      "parity": "N",
      "stopbits": 1,
      "timeout_ms": 500,
      "poll_period_ms": 0,
      "retry_count": -1,
      "slow_poll_period_ms": 30000
    },
    {
      "id": "pp_udp_1",
      "name": "UDP-192.168.66.100",
      "enabled": true,
      "transport": "udp_c2000pp",
      "device_slave_id": 16,
      "local_host": "192.168.66.97",
      "local_port": 40005,
      "remote_host": "192.168.66.100",
      "remote_port": 40005,
      "timeout_ms": 500,
      "poll_period_ms": 0,
      "retry_count": -1,
      "slow_poll_period_ms": 30000,
      "prefix": "0x10",
      "channel": "0x10",
      "seq_start": "0x21D1",
      "seq_persist": true
    }
  ],
  "timeout_ms": 500,
  "poll_period_ms": 1000,
  "retry_count": 3,
  "value_register_base": 30000,
  "status_register_base": 40000,
  "log_max_entries": 1000
}
```

> **Примечание:** Список датчиков хранится в `system_config.json` (см. раздел 6).
> Пер-линейные `poll_period_ms`/`retry_count`/`slow_poll_period_ms` описаны в 3.2.2.2: `0`/`-1` означают «использовать общий параметр»; `retry_count` — число повторов до перевода датчика в «медленный цикл»; `log_max_entries` ограничивает журнал Modbus целиком (записи + TX + RX + обмены), лог перезатирается по кольцу и не может остановить опрос (3.2.2.3).
> Self-адреса (`0.0.0.0`, `localhost`, пустой host) во всех конфигах системы разрешаются в актуальный IP машины автоматически (`shared/net.py`); жёсткий `127.0.0.1` в конфигурации не используется. IP определяется при запуске и переопределяется при его смене в процессе работы.
> Поле «COM-порт» в редакторе линии поддерживает выбор из доступных портов системы (галочка «Выбрать из доступных» → список `GET /api/poller/ports`: на Windows `COMx`, на Linux `/dev/tty*`); tty-имена без пути автоматически дополняются префиксом `/dev/` (`ttyUSB0` → `/dev/ttyUSB0`) на клиенте и при валидации конфигурации.
> Modbus Poller поддерживает старый одноканальный формат `transport/com_port/udp_host/udp_port` только как входную миграцию. При первом сохранении конфигурации старые поля должны быть преобразованы в `poll_ports[0]`, а новые настройки должны сохраняться только в многоканальном формате.
> `use_mock_server` и `mock_server_url` используются визуализацией для тестового контура (запуск/остановка и переход на страницу Mock Server).
> Poller предоставляет отдельную страницу сервиса (`GET /` на порту Poller) для полного управления: старт/стоп, статус всех линий, парный журнал обменов (линия опроса + строка/элемент `system_config.json` -> TX -> RX -> результат), цветовая подсветка линии и типа запроса, отдельные окна TX/RX Modbus в HEX-байтах, полные UDP-датаграммы для `udp_c2000pp`, настройка `device_slave_id` и поиск устройств по Slave ID на выбранной линии.
> Поиск устройств выполняется запросом чтения по адресу `40000` (или `status_register_base`) на выбранной линии `poll_port_id`, с `timeout_ms`; есть ответ — устройство найдено, нет ответа — не найдено.
> Visualizer должен проксировать эти же операции в `/settings/poller`: список линий, состояние manager/workers, старт/стоп/перезапуск одной линии, сохранение линии, удаление свободной линии, scan по выбранной линии и просмотр логов по линии.
